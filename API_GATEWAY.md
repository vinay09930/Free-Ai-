# API GATEWAY
A planned embedded Ktor engine will proxy requests locally over standard OpenAI compatible interfaces.

Route Plan:
`POST /v1/chat/completions` -> Resolves to Room History & Retrofit/llama-cpp implementations.
