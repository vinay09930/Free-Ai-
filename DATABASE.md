# DATABASE
The architecture currently uses Room for local persistance of Chats.

Pending Schema:
- `UserEntity` for profile and settings
- `ProviderEntity` for storing browser provider cookies
- `ModelEntity` for tracking downloaded AI models
